# Example environment configuration for urirun-work.
# Copy to .env and adjust as needed. Never commit real secrets.

# Optional: maximum number of parallel workers (defaults to lane capacity).
# URIRUN_WORK_MAX_WORKERS=4

# Optional: default log level (DEBUG, INFO, WARNING, ERROR).
# URIRUN_WORK_LOG_LEVEL=INFO
